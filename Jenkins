pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name ecinstance1 --template-body file://ec2.yaml --tags Key=Name,Value=128asp01 Key=costid,Value=128 --region 'us-west-2'"
              }
             }
            }
            }

pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name s3bucket2019 --template-body file://s3.yaml --region 'us-east-1'"
              }
             }
            }
            }

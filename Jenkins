pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name sabhi201987 --template-body file://s3.yaml --region 'us-east-1'"
              }
             }
            }
            }
